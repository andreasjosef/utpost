# packages

Shared packages consumed by the apps in `apps/*` (via the `workspace:*` protocol) live here.

Currently empty. The first package planned for this directory is `flags`, once feature flags
become a shared package as part of moving to a trunk-based development flow.
